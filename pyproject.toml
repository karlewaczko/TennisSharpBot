[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tennissharp"
version = "0.1.0"
description = "Surface-Elo + ML tennis match modeling and value-betting analysis"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
