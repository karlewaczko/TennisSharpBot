__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Large raw downloads / derived tables are re-fetched or regenerated by the
# scripts each run -- keep git history light by not committing them. The
# small, human-readable derivatives (current Elo ratings, report summaries)
# are still tracked.
data/raw/
data/processed/matches_normalized.csv
data/reports/backtest_bets.csv

# Local secrets
.env
config/.env

# tennisratio player pages: >1 MB each, re-fetchable, cached locally only
data/raw/tennisratio/
